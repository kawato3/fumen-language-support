{
  "version": "0.2.0",
  "configurations": [{
    "name": "Fumen 拡張を試す",
    "type": "extensionHost",
    "request": "launch",
    "args": ["--extensionDevelopmentPath=${workspaceFolder}", "${workspaceFolder}/examples/basic.fumen"],
    "outFiles": ["${workspaceFolder}/out/**/*.js"],
    "preLaunchTask": "npm: build"
  }]
}
