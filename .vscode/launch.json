{
  "version": "0.2.0",
  "configurations": [{
    "name": "Fumen 拡張を試す",
    "type": "extensionHost",
    "request": "launch",
    "args": ["--extensionDevelopmentPath=${workspaceFolder}", "${workspaceFolder}/docs/examples/basic.fumen"],
    "outFiles": ["${workspaceFolder}/build/extension/**/*.js"],
    "preLaunchTask": "npm: build"
  }]
}
